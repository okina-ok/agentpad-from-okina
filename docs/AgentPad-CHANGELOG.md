# AgentPad 更新日志

> 产品：Codex Micro 平替 RGB 状态键盘（v1 = 通道 B 状态文件协议）
> 当前版本：v0.7.19（2026-08-07，修 PTT UI 卡"转写中"）

## v0.7.19 · 2026-08-07 —— 修 PTT UI 卡"转写中"（编码 + 状态兜底）

### 修复
- ptt.py 启动时强制 stdout/stderr 用 UTF-8 输出：之前子进程管道输出按系统
  GBK 编码，模拟器按 UTF-8 解码，"转写结果 / 没听清 / 已填入"等中文状态标记
  全部匹配不上，UI 一直停在"转写中"。
- ptt.py 的异常信息同时打到 stdout，模拟器能识别 PTT 异常并标红，不再无提示卡住。
- 模拟器放宽完成判定：出现"转写结果 / 注入失败 / 已填入 / 太短 / 没听清"
  任一即回到"就绪"。
- 新增 60 秒兜底：转写超过 60 秒无回音（丢行 / 子进程崩溃）强制回"就绪"，
  杜绝 UI 永久卡死。

### 实测
- 按住语音键 1.2 秒松开：录音开始 -> 转写 -> 返回就绪；
  字节级验证子进程输出为 UTF-8（e8bdac... = 转写结果：（没听清）），
  模拟器 reader 可正确匹配并复位状态。

## v0.7.18 · 2026-08-07 —— 模拟器语音大键接通 PTT（与实体按键同链路）

### 新增
- 模拟器"语音输入"大键接通 `ptt.py --demo`（与 ESP32 实体按键共用同一套
  录音 / faster-whisper 转写 / 注入链路）：
  - 鼠标按住 = 按下实体按键（开始录音）
  - 松开 = 松开实体按键（本地转写 -> 自动填入 Codex 输入框供审阅）
- 语音键状态可视化：模型加载中 / 就绪 / 录音中（红）/ 转写中 / PTT 异常，
  由 ptt.py 输出驱动，20fps 刷新。
- 模拟器关闭时自动结束 PTT 子进程，不残留后台进程。

### 修复
- inject_text.py 最小化窗口的匹配从 "AgentPad 模拟器" 改为 "AgentPad"：
  新版模拟器标题 "AgentPad v1 模拟器 · 4×4 布局" 不再包含连续子串，
  注入前无法自动收起，可能挡住输入框。

### 实测
- 模拟鼠标按住语音键 1.5 秒 -> ptt_result.txt 出现"录音开始 / 录音结束 /
  转写中"，链路与实体按键完全一致（静音环境转写为"没听清"，正确跳过空文本注入）。

## v0.7.17 · 2026-08-07 —— 修 thinking/running 反了：工具信号语义纠正 + 回显行过滤

### 修复
- 纠正 "tool call completed" 的语义：日志里它是工具**开始执行**的回执
  （`execution_started=true`，`handler_duration_ms` 只是 dispatch 耗时），
  不是工具结束。原来被当作"工具完成 -> thinking"，导致工具真正在跑时灯反
  而是紫的；现在与工具行一样按 running 处理。
- thinking 信号补齐：DeepSeek 等构建不写 SSE reasoning 行，但会写
  `Output item item_type="reasoning"`（模型正在生成推理），现作为 thinking 信号。
- `needs_follow_up=true` 不再把状态拉回 thinking（它只是"回合还要继续"的信号，
  常在工具刚派发完时出现，会制造 thinking/running 横跳）；只撤销 done 兜底。
- 过滤 `codex_http_client::transport` 回显行：这类行是发给模型的 HTTP 请求体，
  内含历史对话/项目笔记对旧事件字符串的引用（needs_follow_up=false、
  tool call completed、response.completed 等），会重复触发旧规则；现在整体跳过。
- 效果：thinking（紫）只由 reasoning 输出 / SSE 推理行驱动，running（青）
  只由工具行驱动，工具执行期间不再被拉回紫，状态与真实阶段一一对应。

### 测试
- 状态机单测扩到 18 项（新增：工具完成回执保持 running、transport 回显行跳过），
  全部通过；频道绑定 10 项通过。
- 用真实日志回放"构建酷炫键盘官网"频道：用户输入 -> thinking -> 工具行 -> running
  -> reasoning 输出 -> thinking …… 与工具/推理事件一一对应。

## v0.7.16 · 2026-08-07 —— 4×4 方格布局定稿

### 变更
- 模拟器改为用户拍板的 4×4 方格布局（替代 Codex Micro 仿排布）：
  - 第 1 排：强度旋钮 | 频道1 | 频道2 | 屏幕（只占 1 键位）
  - 第 2 排：频道3 | 频道4 | 频道5 | 频道6
  - 第 3 排：确定 | 返回 | 新建 | 分支（可自定义预设）
  - 第 4 排：停止(可自定义) | 语音输入（2U 大键） | 通话(可自定义)
- 语音大键交互：按住录音 → 松开本地转写并填入对话框 → 按"确定"发送。
- 屏幕改为 1U，实时显示最活跃频道状态 + 电量占位。
- 物理按键数不变（13 键 + 1 旋钮），硬件成本结构基本不变。
- 同步更新 v1 硬件规格（1.5 节）与产品决策看板。

## v0.7.15 · 2026-08-07 —— 模拟器布局对齐 Codex Micro

### 新增
- 模拟器 UI 从通用 2×3+命令排布，改为 Codex Micro 官方键盘的整体布局：
  - 顶行：左摇杆 + 6 颗半透明 Agent 键 + 右旋钮（推理深度，颜色跟随最活跃频道）。
  - 下行：左触摸区（图层切换）+ 1.54" 屏幕占位（我们的差异化卖点，实时显示
    最活跃频道的状态与电量占位）+ 7 颗命令键（接受/拒绝/新建/分支/PTT/停止/重试）。
- 屏幕优先展示 thinking/running/waiting 频道，其次取 fresh=active，再无则显示"就绪"。
- 底部保留灯色图例（紫=思考 青=运行 琥珀=等待 绿=完成 灰=空闲）。
- 全部布局抽成顶部常量（键位尺寸、间隙、配色），后续按真实打板尺寸微调只改一处。

### 修复
- 无（本轮为纯 UI 布局迭代，状态机/绑定逻辑未动）。

## v0.7.12 · 2026-08-07 —— 后台任务不再误判为空闲

### 修复
- 同一提交的重复 user_input dispatch 行现在作为"活动心跳"刷新活动时间，
  真实后台任务（如"构建酷炫键盘官网"）不会再被 10 秒安静判定误判为 idle。
- 效果：后台任务运行时显示 thinking/running（紫/青），完成后 2 秒变绿；
  纯点击仍 10 秒回空闲。

### 实测
- "构建酷炫键盘官网"任务进行中：slot2 实时 running/thinking 切换，
  fresh=active；`--once` 输出与运行中守护进程一致。

## v0.7.11 · 2026-08-07 —— 面板绑定丢失/顺序困惑修复

### 修复
- 守护进程保存 channel_map 前重新读取 manual，不再用旧数据覆盖
  面板刚写入的绑定（读写竞态导致"改了没生效"）。
- 绑定面板每个频道标注模拟器网格位置（上排·左/中/右、下排·左/中/右），
  消除"竖排 1-6 与 2×3 网格对不上"的理解歧义；保存后立即刷新当前映射。

### 实测
- 模拟"频道2 -> 构建酷炫键盘官网"绑定：4 秒后 manual 仍在、显示同步更新，
  其余频道自动让位。

## v0.7.10 · 2026-08-07 —— 槽位压实修复 + 会话标题同步

### 修复
- 启动时清除幽灵/失效槽位记录（旧版本缓存的截断 ID、影子线程残留），
  并把有效会话按原相对顺序压实，消除"4 号位空着、会话挤在 5 号"的洞。
- 新会话进入池后标题立即同步（之前显示成 ID 前缀）。

### 实测
- 用户第 5 个真实对话"构建酷炫键盘官网"正确上槽（4 号），
  其自报 waiting 状态（状态文件机制）正常显示。
- 频道自测 10 项 + 状态机 16 项全过。

## v0.7.9 · 2026-08-07 —— "主频道"概念可配置化

### 变更
- 产品思考：普通用户没有"主频道"概念，6 个频道应完全平等。
  `anchor_channel_1` 默认 **false**（所有频道平等，LRU 逐出不偏袒频道 1）；
  需要"第一频道固定"的用户（含本项目作者）在 `channel_map.json` 里设 `true`。
- 固定某个对话 = 用绑定面板手动绑定（manual 永不逐出），这是给用户统一的"锚定"入口。
- 手册/规格文档同步更新；频道自测扩至 10 项（含锚定开关两种行为）。

## v0.7.8 · 2026-08-07 —— 后台会话响应灵敏度优化

### 优化
- SSE 实时流归属：活跃会话安静超 10 秒且其他会话近期在活动时，
  实时流自动跟到那个会话（后台会话也能"输出即变绿"）。
- 后台会话 done 兜底安静时长 5 秒 → 2 秒（DONE_QUIET_BG）。
- 说明：主窗口永远是活跃会话，实时流（不带线程 ID）只能喂一个会话；
  两会话同时生成时主窗口优先，这是数据源物理限制。

### 实测
- 频道自测 9 项 + 状态机 16 项全过；守护进程重启后 4 槽位正常。

## v0.7.7 · 2026-08-07 —— 修复"6 个槽全满 + 点击就 running/thinking"

### 修复
- **槽位只认 App 会话列表**（~/.codex/state_*.sqlite 的 threads 表）。
  日志里出现但不在 App 会话列表的线程（App 的影子/预览线程）不再占槽，
  解决"只有 4 个对话但 6 个槽全满"。
- **会话池定期刷新（30 秒）**：新开的真实对话自动进入候选池。
- **点击噪声防护**：点击/激活会话产生的假 user_input（只有一条 dispatch、
  之后 10 秒无后续活动）自动回空闲，不再误亮 thinking/running。

### 实测
- 频道自测 9 项 + 状态机 16 项全过；重启后槽位恢复为 4 个真实对话。

## v0.7.6 · 2026-08-07 —— 修复"输出完还卡 thinking" + 绑定面板增强

### 修复
- 恢复 done 双通道兜底（多线程重构时弄丢了）：thinking 且收到过完成信号 +
  安静超时（response.completed 1 秒 / needs_follow_up=false 5 秒）→ 判绿。
- 移除"活跃会话 15 秒无活动就忽略 SSE 流"的激进规则（曾导致输出流漏判、永久卡紫）。
- get_slots 暴露 last_response_ts / last_content_ts 供守护进程判 done。

### 增强（绑定面板易用性）
- 每个频道行实时显示"当前: 会话名 [状态]"，每 2 秒自动同步；
- 新增"刷新会话列表"按钮（新开的对话即时可见）。

### 实测
- 频道自测 9 项 + 状态机 16 项全过；守护进程重启后 Kina / C/D 盘正确显示 done。

## v0.7.5 · 2026-08-07 —— 用户手册草稿

### 新增
- `docs/agentpad-user-manual-draft.md`：面向新用户的使用手册草稿——
  环境自检（只装 Codex 的电脑）/ 安装步骤 / 产品操作 / **频道绑定与切换专题** /
  故障排查；硬件章节留待定稿后补全。

## v0.7.4 · 2026-08-07 —— 频道锚定 + 稳定性自测 + 一键启动

### 修复/加固
- **频道 1 锚定**：频道 1 的会话永不参与逐出（用户要求"第一频道一直在第一频道的位置"）。
- LRU 逐出修正：新会话只有比"当前最不活跃的自动会话"更新才逐出，否则不挤占；
  不再出现"6 号位被一路踢到会话1"的问题。
- 频道绑定文件读取不再依赖文件 mtime（本机 mtime 不更新，实测三次写入同一时间戳），
  改为每次直接读，绑定改动即时生效。
- 自动槽位首次分配即固定，重复调用稳定（自测覆盖）。

### 新增
- `test_channel_bind.py`：频道绑定自测 9 项（首轮分配/稳定/频道1锚定/LRU 逐出/
  手工绑定/重启持久化/取消绑定保留槽位/12 会话压力/截断 ID），连跑 5 轮全过。
- `guard_daemon.py`：守护进程看门狗，status_daemon.py 崩了自动拉起。
- `start_agentpad.ps1` + 桌面快捷方式：一键启动（看门狗 + 模拟器 + 绑定面板）。

### 实测
- 频道绑定自测 9 项 + 状态机单测 16 项，连跑 5 轮全过。
- recency 只随真实活动更新（打开会话/后台杂音不算活跃度），LRU 更准。
- 守护进程实时跟踪当前会话（日志可见 running→thinking→done 循环）。

## v0.7.3 · 2026-08-07 —— 幽灵会话修复（5/6 频道出现未知会话）

### 修复
- 日志中部分行会把会话 ID 截断（如 `thread_id=019fd6a2-46d2`），
  旧正则会把它当成"新会话"，并把当前对话的活动算到它头上，导致 5/6 频道
  出现无名字的假会话。现在只认完整 UUID（8-4-4-4-12），启动时自动清理幽灵。
- submission.id 同样只认完整 UUID，避免同提交被误判为多条新消息。

## v0.7.2 · 2026-08-07 —— 频道绑定机制（解决"串了"与映射漂移）

### 修复
- 活跃会话判定：只有**真实用户消息**（新 submission）才切换活跃会话，
  后台工具行不再抢焦点，SSE 流不再喂错对象（修"状态串到别的会话"）。
- 槽位映射：手工绑定（channel_map.json / bind_panel.py）优先且永不逐出；
  自动分配首次出现时定槽、之后不挪位，选中其他对话不再导致频道重排。
- 模拟器不再显示 STALE/SUSPECT 这类"新鲜度"字样；状态色保持，超时自动变暗。

### 新增
- `bind_panel.py`：本地图形面板，把对话绑定到 1-6 频道，
  **不需要跟 AI 交流**；保存后守护进程 1 秒内生效。
- `channel_map.json`：频道绑定文件（manual=手工 / auto=自动快照）。
- display_state_v2.json 增加 thread_id 字段，方便对账。

### 说明
- 数据事实：只有用户真正在某个会话发消息/切过去，那个会话才有新日志；
  没动过的会话保持空闲（灰）是正常的，不是串了。

## v0.7.1 · 2026-08-07 —— 多会话状态机（多条对话同时监听）

### 新增
- `log_state.py` 新增 `MultiLogTracker`：每会话一个独立状态机，
  行路由规则 = 带 thread_id 的 trace 行喂给对应会话，SSE 流喂给当前活跃会话。
- 会话元数据（标题/活跃度/钉选）来自 `~/.codex/state_*.sqlite` 的 threads 表；
  槽位 = 最近活跃的 N 个会话（LRU），旧会话 6 小时内无活动不占槽。
- `status_daemon.py` 输出 6 个真实会话槽位（slot/state/color/effect/name），
  文件 waiting 覆盖到活跃槽位，逐槽防抖。
- `simulator.py` 键帽显示会话名。
- 单测 16 项全过（LogStateTracker 单会话行为保持不变）。

### 实测（2026-08-07）
- 本机 4 条对话全部上槽：构想 Codex 硬件语音按键(running) / hi /
  认识新用户Kina / 我c,d爆满了，你仔细看…
- 活跃会话状态实时联动（当前对话跑工具 → 青色）。

### 待办（第 6 步剩余）
- 接 13 颗 WS2812 灯条（固件改 LED_COUNT=13）
- 钉选（app threads 表已有 is_pinned 字段，软件层未接）
- 1.54" 屏显示会话列表

## v0.7.0 · 2026-08-07 —— 灯珠原型链路打通（路线图第 6 步开始）

### 新增
- `bridge.py`：串口桥，读 display_state_v2.json → 单行 JSON 帧下发（协议 v0.1，
  内容变化才发、ACK 确认 + 3 次重发、半包缓冲）。
- `led_firmware.py`：ESP32-C3 MicroPython 固件（上传为板子 /main.py），收帧驱动
  WS2812（原型用板载 GPIO8 单灯），灯效 solid/breathe/flash/off，同时上报按键事件。
- `upload_main.py` 支持 `--src / --port` 参数。
- v1 规格锁定：6 agent + 7 命令 + 旋钮 + **1.54" IPS 屏**，主控 ESP32-C3，
  嘉立创全 SMT 免焊（见 docs/v1-hardware-plan.md）。

### 实测（2026-08-07 02:30+）
- status_daemon → bridge → COM5 → 板载灯 全链路 ACK 通过；
- 测试模式 6 种状态循环驱动正常：紫呼吸 / 青常亮 / 琥珀闪 / 绿 / 红闪 / 灰。

### 待办（第 6 步剩余）
- 接 13 颗 WS2812 灯条（固件改 LED_COUNT=13）
- 多线程状态机（6 会话 LRU + 钉选）
- 1.54" 屏显示会话列表

## v0.6.2 · 2026-08-07 —— 调研参考项目 agentpad13

### 新增
- `docs/agentpad13-research.md`：开源 13 键键盘 agentpad13 完整调研
  （硬件/固件/Raw HID 状态协议/成本/许可证），结论：不直接采用，借鉴协议设计。
- 记录关键发现：其主机守护程序 loudestd 未开源，状态检测侧需自研（我们已有）。

## v0.6.1 · 2026-08-07 —— 公开仓库整理

### 新增
- README 增加"项目背景 / 当前进度一览"，方便访客快速了解项目定位与阶段。
- 核心代码补齐模块文档与关键逻辑注释（serial_listen.py 等）。
- 确认 .gitignore 覆盖语音模型（约 460MB）、固件、日志等大文件，
  仓库体积可控，模型按 README 指引自行下载。

## v0.5.0 · 2026-08-07 —— PTT 语音原型（硬件链路打通）

### 新增
- `ptt.py`：PTT 语音输入原型。板载 BOOT 键按住 = 录音，松开 = 本地转写。
- 实测通过：按住 BOOT 说"帮我写一个快速排序算法"，松开后 2 秒转写出正确文字，
  全程离线（faster-whisper small 模型，约 460MB，本地加载）。
- 硬件链路确认：ESP32-C3 按键 -> 串口 JSON -> PC 录音 -> 本地转写。

### 修复
- ptt.py 缺失 faster_whisper / sounddevice import，已补；
- 增加错误日志（异常写入 ptt_result.txt），问题可定位。

## v0.6.0 · 2026-08-07 —— 语音转文字自动填入 Codex 输入框

### 新增
- `inject_text.py`：UIA 定位 Codex 桌面端输入框控件（窗口不全屏/移动过也准），
  剪贴板 + Ctrl+V 注入，粘贴后读回输入框内容验证（verify OK）。
- ptt.py 转写完成后自动派生子进程注入（后台进程直接调 UI 自动化会被 Windows
  限制焦点，子进程方式已实测通过）。
- 实测（01:06）：按住 BOOT 说"明天下午三点开周会"，松开后文字自动进输入框，
  审阅后可发送。PTT 语音输入全链路完成。

---

## v0.4.0 · 2026-08-07 —— 输出阶段修正 + 延迟优化收尾（验收版）

### 修复：最终回答输出时灯一直停留在 thinking（用户反馈）
- `response.output_text.delta`（回答文字开始流式出现）→ 直接判 **done（绿）**，
  回答一出来灯就绿，不再等输出完 + 5 秒安静期。
- 输出后的杂项行（response.completed / message item / needs_follow_up）不再把绿拉回紫。
- 工具行（含输出中途）一到立即转 running（青）。
- 说明：agent 边干活边写分析（中间文字输出）也会短暂显示绿色，属同一"输出中"语义。

### 修复：done 兜底（上一版发现 app 不写最终文本 response.completed 行）
- 新增双通道 done 判定：
  * 快通道：response.completed（文本）+ 安静 1 秒 → 绿；
  * 兜底通道：助手文本消息完成 / post sampling `needs_follow_up=false` + 安静 5 秒 → 绿，
    工具行一到立即撤销兜底。

### 优化：转场速度（实测）
- 工具结束 → thinking：3~4 秒 → **约 1 秒**（"tool call completed" 行改判 thinking）
- 防抖 1.5 → 0.5 秒；done 安静期 2.5 → 1.0 秒（兜底 5 秒）；轮询 0.3 → 0.2 秒
- 工具执行期间 ~/.codex 无实时文件写入（实测），1.5~2 秒日志落盘是抓日志方案的物理下限

### 新增
- 状态机调试日志 `log_state_debug.log`（有界 200KB），便于排查状态异常
- 单元测试 16 项（状态机全路径）

---

## v0.3.0 · 2026-08-06 深夜 —— 状态机重写（running 可见性 + 防误判）

### 修复：running 全程看不见（核心）
- 根因：`op.dispatch.user_input` 在工具 dispatch 时也写入，旧逻辑误判为新回合 thinking，
  工具一启动灯就变紫；工具周期的 response.completed 也被误当回合结束。
- 重写判定优先级：工具行(running) > response.completed > 真实用户消息 > 生成中(thinking)
- 工具行识别：ToolCall: / tool call completed / function_call 相关 SSE 与 trace /
  `function_call_arguments.delta`（模型生成工具参数时实时流式落盘，最早的 running 信号）
- 实测：18 秒 / 12 秒长命令全程青色，命令结束后 1~3 秒回紫

### 修复：假"用户消息"（submission.id 去重）
- 根因：`op.dispatch.user_input` 前缀出现在 post sampling token usage /
  Output item message/reasoning 等非用户消息上。
- 修法：只有"新的 submission.id"才算用户发消息；同 submission 内一律忽略；
  启动时预填最近 submission.id，重启不误触发。

### 新增
- 自动绑定最近使用的会话线程，其他会话的工具 trace 行不干扰，换会话自动跟随
- 参考案例调研：ThreadBeacon（同款日志抓取，承认工具执行期会失明）、
  AgentDiode（走官方 hooks，官方对 waiting 也无事件）、codex-micro-device（LED 协议专有无文档）、
  M5Stack Core2 开源固件（v2 HID 参考）、cmux issue（官方状态也过期）

---

## v0.2.0 · 2026-08-06 —— 日志被动检测（M2）

- 从"agent 自觉写状态文件"改为被动读取 Codex 桌面端事件日志
  （`~/.codex/logs_2.sqlite`），推导 thinking / running / done。
- 修复 done↔thinking 横跳：工具 dispatch 的 user_input 误判、防抖器首次返回 None 崩溃、
  旧守护进程残留写同一显示文件。
- 显示文件隔离为 `display_state_v2.json`。
- 回合结束判定：turn/completed 按内部迭代触发不可用，改"安静判定"。

---

## v0.1.0 · 2026-08-06 —— M1 原型

- 状态灯模拟器（置顶窗口，6 Agent 键 + 7 命令键 + 旋钮）
- 守护进程雏形：轮询 `.agent-status/agent-N.json`，输出灯效 JSON。
- 灯色约定：紫=thinking、青=running、琥珀=waiting、绿=done、灰=idle。

---

## 已知限制（记录在案）

- 日志 SSE 行不带线程归属，多会话同时活跃时次要会话事件可能短暂影响灯色。
- 日志库格式非稳定公开 API，Codex 升级后可能需要微调匹配串。
- waiting 审批态无法从日志被动检测，靠 agent 自觉上报 + 心跳兜底。
- 长回复时 app 侧最终文本完成行可能延迟/缺失（已用兜底通道覆盖）。
