# AgentPad 产品决策与迭代看板

> 这是"我们俩"协调想法的单一入口：你随时用文字/语音丢想法进来，
> 我会归类到下面对应位置；待定项一旦拍板，写进更新日志。

## 协作约定

- 每条决策带状态：✅ 已锁定 / 🔶 待定 / ❓ 争议中
- 你提的想法即使很小也会记下来（不丢想法）
- 争议项我会给出建议 + 理由，最终由你拍板

## 已锁定决策（✅）

| 时间 | 决策 |
| --- | --- |
| 08-07 | 通道 B（本地日志 + 状态文件）为状态检测主方案，不依赖官方私有协议 |
| 08-07 | v1 布局：6 agent + 7 命令 + 旋钮 + **1.54" IPS 屏**（占 2U） |
| 08-07 | 主控 ESP32-C3；成品嘉立创全 SMT 免焊，单台成本约 ¥110 |
| 08-07 | 频道绑定：`channel_map.json` + 绑定面板；手工绑定永不逐出 |
| 08-07 | 锚定可配置（`anchor_channel_1`）：**默认关**（普通用户无主频道），本机开 |
| 08-07 | 槽位规则：只认 App 会话列表；自动分配首次定槽、之后不挪位 |
| 08-07 | v1 不做的：摇杆、触摸、VIA/Vial、电池 |
| 08-07 | 语音转写：faster-whisper small 本地离线（备选 Paraformer/medium 待测） |

## 待定 / 争议（🔶 ❓）

- ❓ 售价与售卖方式：闲鱼小批量几台？单价？→ 建议先市场验证再定
- ❓ 命令键功能：接受/拒绝/新建/分支/停止/重试，哪些真正实现（依赖 UI 自动化）
- ❓ 频道切换：按键盘跳转 Codex 对话的技术方案（UIA 侧边栏读不到，需另找路子）
- ❓ 外壳材质/配色：等硬件打样拿到手再定
- ❓ 定制服务：开机动画/表情包是否作为卖点
- ❓ 语音准确率：换 Paraformer（中文更强）还是 medium（省事）
- ❓ 一键自检脚本 check_setup.py 的交付形态（随安装包）

## 迭代看板

### 📥 Backlog（待做）
- 第 6 步剩余：13 颗 WS2812 灯条接入、1.54" 屏显示会话列表
- 一键自检脚本 check_setup.py
- 打包安装包（PyInstaller + Inno Setup，自动发现 COM/日志库）
- 命令键动作映射（接受/拒绝/停止/重试）

### 🔄 进行中
- 多会话状态 + 频道绑定：软件侧已通，等用户实测反馈
- 用户手册草稿补全（硬件章节待定稿）

### 🧪 待验证
- 后台会话响应灵敏度（v0.7.8，等待实测确认）
- 绑定面板选中即自动保存（v0.7.11+，等待实测确认）

### ✅ 已完成
- 详见 `AgentPad-CHANGELOG.md`（当前 v0.7.11）

## 下一里程碑建议

1. 功能软件闭环验收（你实测 1-2 天）
2. 闲鱼/小红书发帖做市场验证（文案我可以写）
3. 嘉立创打板（全 SMT）+ 外壳结构打样
4. 外观精修（键帽/配色/灯效）——最后做
